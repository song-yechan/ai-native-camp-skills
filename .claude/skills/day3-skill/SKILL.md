---
name: day3-skill
description: "AI Native Camp Day 3 — 스킬 설계와 제작: Clarify + SKILL.md. '/day3', '스킬 설계', '스킬 만들기' 요청 시 사용."
---

# Day 3: 스킬 설계와 제작

> **테마**: Context 활용 — 맥락 기반 워크플로우 구축
> **핵심 메시지**: "완벽하게 만들려 하지 마세요. 일단 만들고, 쓰면서 고치면 됩니다."
> **예상 소요**: 약 75분
> **사전 과제**: Day 2 MCP 연결 (또는 개념 이해)
> **산출물**: 나만의 스킬 SKILL.md 1개

---

## 진행 가이드 (Claude가 따를 지시)

### 인터랙션 규칙
1. **한 블록씩 순서대로** 진행한다. 블록을 건너뛰지 않는다.
2. 각 블록의 **EXPLAIN → EXECUTE → QUIZ** 순서를 지킨다 (없는 단계는 생략).
3. EXPLAIN은 **핵심 비유와 개념만 압축**해서 전달한다. 상세 내용은 "references/concepts.md 참조"로 안내한다.
4. EXECUTE는 사용자가 **직접 따라할 수 있도록** 구체적 명령어와 기대 결과를 보여준다.
5. QUIZ는 **AskUserQuestion** tool로 객관식 3문항을 출제한다. 정답 시 다음 블록, 오답 시 해설 후 재시도.
6. 각 블록 완료 후 **"다음 블록으로 넘어갈까요?"** 라고 물어보고 사용자 응답을 기다린다.
7. 모든 응답은 **한국어**로 한다.
8. 사용자가 "다음", "넘어가자", "계속" 등으로 응답하면 다음 블록을 시작한다.
9. 사용자가 "자세히", "더 알려줘" 라고 하면 references/concepts.md의 해당 섹션을 읽어서 상세 설명한다.

### 진행 상태 표시
각 블록 시작 시 아래 형식으로 진행 상태를 보여준다:
```
📍 Day 3 진행: [Block N/4] — 블록 제목
```

---

## Block 0: 스킬 설계 원칙

### EXPLAIN

📍 Day 3 진행: [Block 0/4] — 스킬 설계 원칙

**스킬 = AI에게 주는 업무 레시피**

요리 레시피처럼:
- "이 재료들을 이 순서로 조합하면 이 요리가 나와"
- 재료와 순서만 따라하면 **누가 해도 같은 결과**

**CLAUDE.md vs Skill:**

| 구분 | CLAUDE.md | Skill |
|------|-----------|-------|
| 비유 | 업무 매뉴얼 | 요리 레시피 |
| 로딩 | **항상** (매 세션) | **필요할 때만** |
| 내용 | "나는 이런 사람" | "이 일은 이렇게" |

**좋은 스킬의 3가지 조건:**

1. **목적이 명확하다** — 한 문장으로 설명 가능
   - 나쁜 예: "이메일 관련 여러 가지를 한다"
   - 좋은 예: "Gmail에서 뉴스레터를 찾아 인사이트를 정리한다"

2. **입력과 출력이 분명하다**
   - 입력: 무엇을 받는가? (키워드? 파일? URL?)
   - 출력: 무엇을 내놓는가? (md 파일? JSON? Slack 메시지?)

3. **실행 단계가 구체적이다**
   - 나쁜 예: "이메일을 적절히 정리한다"
   - 좋은 예: "1. 키워드 검색 → 2. 상위 5개 선택 → 3. 핵심 추출 → 4. md 파일 생성"

> 상세 내용은 `references/concepts.md` 섹션 2~3을 참조하세요.

### QUIZ

**Q1. Skill이 "필요할 때만 로딩"되는 이유는?**
- (A) 파일 크기가 너무 커서
- (B) 컨텍스트 윈도우가 유한하므로 효율적으로 사용하기 위해 ✅
- (C) 보안상의 이유로
- (D) Claude가 읽기 어려워서

해설: 컨텍스트 윈도우는 유한합니다. CLAUDE.md는 항상 필요하지만, Skill은 호출될 때만 필요합니다.

**Q2. 좋은 스킬의 3가지 조건이 아닌 것은?**
- (A) 목적이 명확하다
- (B) 입력과 출력이 분명하다
- (C) 코드가 포함되어야 한다 ✅
- (D) 실행 단계가 구체적이다

해설: 코드 없이 SKILL.md 텍스트만으로도 스킬이 됩니다. 핵심은 목적, 입출력, 실행 단계입니다.

**Q3. 스킬에서 "적절히", "알아서" 같은 표현이 문제인 이유는?**
- (A) 한국어 문법이 틀려서
- (B) Claude가 매번 다르게 해석하여 결과가 일관되지 않으므로 ✅
- (C) 파일 용량이 커져서
- (D) 실행 속도가 느려져서

해설: 모호한 표현은 Claude가 매번 다르게 해석합니다. 구체적으로 적어야 일관된 결과를 얻습니다.

---

## Block 1: Clarify — 모호함을 명확함으로

### EXPLAIN

📍 Day 3 진행: [Block 1/4] — Clarify

**Clarify = 모호함을 명확함으로 바꾸는 과정**

```
Before: "이메일 정리해줘"
              ↓ Clarify
After:  "Gmail에서 'AI' 키워드 뉴스레터를 찾아서,
        핵심 인사이트를 요약하고,
        insights/YYYY-MM-DD.md로 저장해줘"
```

**왜 필요한가?**
- 우리 머릿속 의도는 모호한 경우가 많음
- AI는 모호한 입력 → 추측해서 실행 → 높은 확률로 틀림
- Clarify로 **추측 대신 질문하게** 만들기

**3가지 Clarify 스킬:**

| 스킬 | 용도 | 명령어 |
|------|------|--------|
| **Vague** | 모호함 해결 (가장 기본) | `/clarify-vague` |
| **Unknown** | 블라인드 스팟 찾기 | `/clarify-unknown` |
| **Meta** | 내용/형식 분리 검토 | `/clarify-meta` |

**최적의 타이밍: 플래닝 직후!**
```
1. 요청 입력
2. Claude가 계획을 만듦 (Shift+Tab)
3. ⭐ 여기서 Clarify! ⭐
4. 계획 수정 → 실행
```

> 상세 내용은 `references/concepts.md` 섹션 1을 참조하세요.

### EXECUTE

**Clarify 체험**

아래 모호한 요청을 Claude에 입력해보세요:
```
우리 팀 업무를 개선해줘
```

Claude가 바로 실행하려 할 것입니다. 이때 아래를 입력하세요:
```
잠깐, 모호한 부분을 먼저 질문해줘. 어떤 팀인지, 어떤 업무인지, 개선 기준이 뭔지 등.
```

→ Claude가 Clarify 질문을 하기 시작합니다. 질문에 답하면서 요구사항이 구체화되는 과정을 체험하세요.

---

## Block 2: 스킬 제작 1 — 문제를 스킬 언어로 번역

### EXPLAIN

📍 Day 3 진행: [Block 2/4] — 스킬 제작 1: 번역 + Clarify

**Step 1: 문제를 스킬 언어로 번역**

자연어 문제 → **입력 → 처리 → 출력**으로 구조화

```
자연어: "뉴스레터가 너무 많아서 다 못 읽어요.
        중요한 인사이트만 정리하고 싶어요."

↓ 번역

스킬 언어:
  입력: Gmail에서 특정 키워드가 포함된 이메일
  처리: 각 이메일에서 핵심 인사이트 추출
  출력: insights/YYYY-MM-DD.md 파일
```

**팁**: "입력 → 처리 → 출력"으로 정리하면 스킬이 보입니다.

### EXECUTE

**나만의 문제 번역하기**

사용자에게 AskUserQuestion으로 질문:
- "스킬로 만들고 싶은 반복 업무나 불편한 점이 있나요? 자유롭게 적어주세요."

사용자 답변을 받은 후, Claude가 **입력 → 처리 → 출력**으로 번역하여 보여준다.

예시:
```
💡 번역 결과:

[원래 문제]
"매주 팀 미팅 전에 각 도구에서 진행 상황을 확인하고 안건을 정리하는 게 번거로워요"

[스킬 언어]
- 입력: 주간 미팅 날짜, 관련 도구 (Slack, Linear)
- 처리: 1. Slack에서 주요 논의 수집 2. Linear에서 진행 상태 수집 3. 안건 형식으로 정리
- 출력: meetings/YYYY-MM-DD-agenda.md
```

**Step 2: Clarify로 모호함 제거**

Claude에게 요청:
```
방금 번역한 스킬 계획에서 모호한 부분을 질문해줘
```

Claude의 질문에 답변하여 요구사항을 구체화한다.

---

## Block 3: 스킬 제작 2 — SKILL.md 생성 + 테스트

### EXPLAIN

📍 Day 3 진행: [Block 3/4] — 스킬 제작 2: 생성 + 테스트

이제 Clarify로 구체화한 내용을 바탕으로 실제 SKILL.md를 만듭니다.

**SKILL.md 필수 구성:**

```markdown
---
name: skill-name          # 영문 소문자 + 하이픈
description: 스킬 설명     # 한 문장 + 호출 트리거
---

# 스킬 제목

## 목적
한 문장으로 무엇을 해결하는지

## 입력
- 입력 1: 설명
- 입력 2: 설명

## 실행 단계
1. 첫 번째 단계 (구체적으로)
2. 두 번째 단계
3. 세 번째 단계

## 출력
- 파일: 경로/파일명
- 형식: 설명
```

**스킬 폴더 구조:**
```
.claude/skills/my-skill/
├── SKILL.md          # 필수: 스킬의 본체
├── scripts/          # 선택: 실행할 코드
├── references/       # 선택: 참고 문서
└── assets/           # 선택: 템플릿, 리소스
```

### EXECUTE

**Step 1: SKILL.md 생성**

Claude에게 요청:
```
지금까지 정리한 내용으로 [스킬명] 스킬을 만들어줘.
.claude/skills/[스킬명]/SKILL.md 에 저장해줘.
```

**Step 2: 생성된 SKILL.md 확인**

확인할 체크리스트:
- [ ] frontmatter에 `name`과 `description`이 있는가?
- [ ] 목적이 한 문장으로 명확한가?
- [ ] 입력과 출력이 분명한가?
- [ ] 실행 단계에 "적절히", "알아서" 같은 모호한 표현이 없는가?

**Step 3: 테스트 실행**

```
방금 만든 [스킬명] 스킬을 실행해줘
```

**Step 4: 결과 확인 + 수정**

결과를 보고 개선할 점이 있으면:
```
이 스킬의 [구체적 부분]을 [원하는 방식]으로 수정해줘
```

> 완벽하지 않아도 괜찮습니다! v1을 만든 것 자체가 성과입니다.
> Day 4에서 "쓰면서 고치기" (Compound Engineering)를 배웁니다.

### QUIZ

**Q1. SKILL.md의 frontmatter에 반드시 포함해야 하는 것은?**
- (A) 작성 날짜와 저자
- (B) name과 description ✅
- (C) 버전 번호
- (D) 라이선스 정보

해설: frontmatter의 name(스킬 식별자)과 description(설명+호출 트리거)이 필수입니다.

**Q2. 스킬을 호출하는 방법이 아닌 것은?**
- (A) /스킬이름 으로 직접 호출
- (B) 자연어로 요청하여 description 매칭
- (C) CLAUDE.md에 적어서 자동 실행 ✅
- (D) "스킬이름 스킬 실행해줘" 명시적 호출

해설: CLAUDE.md에 적는다고 스킬이 자동 실행되지는 않습니다. 스킬은 호출해야 실행됩니다.

**Q3. 스킬 제작 후 가장 중요한 다음 단계는?**
- (A) 문서화하기
- (B) 팀에 공유하기
- (C) 실제로 실행하고 결과를 확인하기 ✅
- (D) 코드를 최적화하기

해설: 만들고 나서 실제로 실행해보고, 결과를 확인하고, 개선하는 과정이 가장 중요합니다.

---

## Block 4: 스킬 체이닝

### EXPLAIN

📍 Day 3 진행: [Block 4/4] — 스킬 체이닝

**스킬 체이닝 = 하나의 스킬 결과를 다른 스킬의 입력으로 연결**

**비유: 공장 생산라인**
```
[원자재] → [1공정] → [2공정] → [3공정] → [완성품]
```

각 공정이 독립적이면서, 앞 공정의 결과물을 다음 공정이 처리합니다.

**실전 예시:**
```
[Gmail] → [newsletter-curator] → [insight-tagger] → [weekly-digest] → [Slack]
           뉴스레터 수집         주제별 태그 분류    주간 리포트 통합    팀 공유
```

**체이닝 3대 원칙:**
1. **한 스킬 = 한 가지 일** — "이것도 저것도" ❌ → "이것만 잘" ✅
2. **입출력 맞추기** — 스킬 A의 출력 형식 = 스킬 B의 입력 형식
3. **중간 결과물 저장** — 각 단계 결과를 파일로 저장 (디버깅 용이)

> 상세 내용은 `references/concepts.md` 섹션 4를 참조하세요.

### EXECUTE

**체이닝 설계 체험**

사용자에게 AskUserQuestion으로 질문:
- "방금 만든 스킬의 결과물을 다른 용도로 활용할 수 있을까요? (예: 요약 → 공유, 분석 → 리포트 등)"

답변을 받은 후:
```
방금 만든 [스킬A]의 출력을 입력으로 받아서 [다음 처리]를 하는
[스킬B] 스킬의 골격을 설계해줘. 아직 파일로 만들지는 말고 설계만 보여줘.
```

→ 체이닝 구조를 시각적으로 보여준다:
```
[스킬A] → 출력: report.md → [스킬B] → 출력: summary-slack.md
```

> 실제로 스킬B를 만들지 않아도 괜찮습니다. 체이닝의 **개념**을 이해하는 것이 목표입니다.

### QUIZ

**Q1. 스킬 체이닝의 핵심 원칙 "한 스킬 = 한 가지 일"의 이유는?**
- (A) 파일 크기를 줄이기 위해
- (B) 재사용과 조합이 쉬워지므로 ✅
- (C) Claude가 긴 스킬을 못 읽어서
- (D) 실행 속도가 빨라져서

해설: 한 가지 일을 잘하는 스킬 여러 개를 조합하면, 다양한 워크플로우를 유연하게 만들 수 있습니다.

**Q2. 체이닝에서 중간 결과물을 파일로 저장하는 이유는?**
- (A) 보안을 위해
- (B) 문제 발생 시 디버깅이 쉬워지므로 ✅
- (C) 파일이 더 안전해서
- (D) Claude가 파일만 읽을 수 있어서

해설: 중간 결과물이 파일로 남아있으면, 어느 단계에서 문제가 생겼는지 쉽게 파악할 수 있습니다.

**Q3. 스킬 체이닝 실행 방법 중 초보자에게 추천하는 것은?**
- (A) 모든 스킬을 한 번에 연결해서 실행
- (B) 단계별로 실행하면서 중간 결과를 확인 ✅
- (C) 가장 마지막 스킬만 실행
- (D) 자동 스케줄로 실행

해설: 처음에는 단계별로 실행하면서 중간 결과를 확인하는 게 안전합니다.

---

## 마무리

### 오늘 배운 것 정리

| 개념 | 핵심 |
|------|------|
| 스킬 설계 원칙 | 목적 + 입출력 + 실행 단계가 명확해야 함 |
| Clarify | 모호함 → 명확함 (Vague / Unknown / Meta) |
| 스킬 제작 3단계 | 번역 → Clarify → SKILL.md 생성 + 테스트 |
| 스킬 체이닝 | 스킬A 출력 → 스킬B 입력 (공장 라인) |
| 플랜 모드 | Shift+Tab으로 계획 먼저 → Clarify → 실행 |

### 오늘의 산출물
- 나만의 스킬 SKILL.md 1개
- 스킬 체이닝 설계 (개념)

### 핵심 마인드셋
> "완벽하게 만들려고 하지 마세요.
> 일단 만들고, 쓰면서 고치면 됩니다.
> 스킬은 v1 → v2 → v3... 계속 진화합니다."

### 다음 시간 예고
**Day 4: 시스템화** — Compound Engineering으로 만든 것을 복리로 키우는 법, GitHub/Vercel/Supabase로 서비스화를 배웁니다.

> Day 3 완료를 축하합니다! 🎉
> Day 4를 시작하려면 `/day4` 를 입력하세요.
